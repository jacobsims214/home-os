apiVersion: v1
kind: Service
metadata:
  name: home-os-ui
  namespace: {{ .Values.namespace }}
spec:
  ports:
  - port: {{ .Values.ui.port }}
    targetPort: {{ .Values.ui.port }}
    protocol: TCP
    name: http
  selector:
    app: home-os-ui
