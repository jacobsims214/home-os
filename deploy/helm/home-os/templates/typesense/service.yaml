apiVersion: v1
kind: Service
metadata:
  name: home-os-typesense
  namespace: {{ .Values.namespace }}
spec:
  ports:
  - port: {{ .Values.typesense.port }}
    targetPort: {{ .Values.typesense.port }}
    protocol: TCP
    name: http
  selector:
    app: home-os-typesense
