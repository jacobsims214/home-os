apiVersion: v1
kind: Service
metadata:
  name: home-os-tika
  namespace: {{ .Values.namespace }}
spec:
  ports:
  - port: {{ .Values.tika.port }}
    targetPort: {{ .Values.tika.port }}
    protocol: TCP
    name: http
  selector:
    app: home-os-tika
